<!DOCTYPE html>
<html lang="en">

<head>
  <title>Create Quiz</title>

  <meta charset="UTF-8" >
  <meta http-equiv="X-UA-Compatible" content="IE=edge" >
  <meta name="viewport" content="width=device-width, initial-scale=1.0" >

  <link rel="stylesheet" href="/vendor/normalize-4.1.1.css" />
  <link rel="stylesheet" href="/vendor/border-box.css" />
  <link rel="stylesheet" href="/styles/main.css" />
  <link rel="stylesheet" href="/styles/layout.css" />
  <link rel="stylesheet" href="/styles/header.css"/>

  <script src="/vendor/jquery-3.0.0.js"></script>
  <script defer src="/scripts/app.js"></script>
</head>

<body>
  <%- include('partials/_header') %>
  <div class="scoreboard">
    <h1>Create Quiz</h1>
    <form method="POST" action="/create">
      <div>
          <label>Title of the Quiz:</label>
          <input type="text" name="quiz_title" placeholder="Enter the Title of the Quiz">
          <label>Hidden Quizes</label>
          <input type="checkbox" name="isHidden">
        </div>
        <% for (let i =1; i <= 5; i++ ) {%>
        <label>Question <%= i %></label>
        <input type="text" name="question_<%= i %>" placeholder="Enter the Question">
        <div>
          <% for (let j = 1; j <= 4; j++ ) { %>
          <input type="hidden" name="cb_<%= i %><%= j %>" value="0">
          <input type="checkbox" name="cb_<%= i %><%= j %>">
          <input type="text" name="as_<%= i %><%= j %>" placeholder="Answer">

          <% } %>
        </div>
     <% }; %>

      <button type="submit" class="Create">Create</button>
    </form>
    </div>
</body>

</html>
